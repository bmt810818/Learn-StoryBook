import type { Meta } from '@storybook/react'
import { Button } from './Button'

// Naming stories
const meta: Meta<typeof Button> = {
    title: 'Button',
    component: Button,
};

export default meta;